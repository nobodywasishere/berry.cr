require "../spec_helper"

describe Berry::REPL do
  pending "REPLs" do
    vm = Berry::VM.new

    # # No way to interact with the REPL
    # # due to limitations of changing what object
    # # the REPL is reading from
    # Berry::REPL.run(vm)
  end
end
